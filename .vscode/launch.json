{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/node_modules/ansi-regex/package.json",
                "localRoot": "${workspaceFolder}/node_modules/ansi-regex"
            }
        }
    ]
}